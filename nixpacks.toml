[phases.setup]
nixPkgs = ["python39", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd KantoCollect/backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
